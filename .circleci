version: 2.1
jobs: 
  test:
    docker:
      - image: circleci/node:10.16.2
    steps:
      - checkout
      - run:
          command: cd frontend && yarn install
workflows:
  tests:
    jobs:
      - test